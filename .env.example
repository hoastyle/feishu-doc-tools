# Feishu Doc Tools (formerly md-to-feishu)
# ========================================
# Feishu App Credentials (Required)
# Get these from https://open.feishu.cn/open-apis/app_modal

FEISHU_APP_ID=cli_xxxxx
FEISHU_APP_SECRET=xxxxx

# Optional: Authentication mode (tenant or user)
# Default: tenant (应用身份认证)
# Options:
#   - tenant: Use tenant_access_token (应用身份，适合自动化操作)
#   - user: Use user_access_token (用户身份，适合用户特定操作)
#
# FEISHU_AUTH_MODE=tenant
#
# Note: If using user mode, you must also set FEISHU_USER_REFRESH_TOKEN
# Run: uv run python scripts/setup_user_auth.py
# to set up user authentication

# Optional: User refresh token (required if FEISHU_AUTH_MODE=user)
# This is automatically generated by the setup_user_auth.py script
# FEISHU_USER_REFRESH_TOKEN=ur-xxxxx

# Optional: Default folder for document creation
# When creating new documents, use this folder by default instead of root
# FEISHU_DEFAULT_FOLDER=fldcnxxxxx

# Optional: Default wiki space
# When creating wiki nodes, use this space by default
# FEISHU_DEFAULT_WIKI_SPACE=123456

# Optional: Batch processing size
# Maximum number of blocks to create in a single API call (default: 200)
# FEISHU_BATCH_SIZE=200

# Optional: Server port (for MCP mode)
# PORT=3333
